# PowerShell doesn't natively support .env files, but you can set them manually:
$env:GOOGLE_CLIENT_ID = "your-client-id"
$env:GOOGLE_CLIENT_SECRET = "your-secret"
$env:SECRET_KEY = "your-flask-secret"